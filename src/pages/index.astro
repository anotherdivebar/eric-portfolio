---
import Base from "../layouts/Base.astro";
import { getCollection } from "astro:content";

const caseStudies = (await getCollection("case-studies"))
  .filter((study) => study.slug !== "kansas-electric")
  .sort((a, b) => a.data.order - b.data.order)
  .slice(0, 2);

const technicalProjects = (await getCollection("technical-projects"))
  .sort((a, b) => a.data.order - b.data.order)
  .slice(0, 2);
---

<Base>
  <!-- Hero: set the tone and proof fast -->
  <section class="py-16 sm:py-20">
    <div class="page-container grid gap-10 lg:grid-cols-[1.2fr_0.8fr]">
      <div>
        <p class="eyebrow mb-4">
          Senior Operations | Technical Program Management | GTM / RevOps
        </p>
        <h1 class="text-4xl font-semibold leading-tight text-ink sm:text-5xl">
          I build operating systems that hold up in production.
        </h1>
        <p class="mt-4 max-w-xl text-lg text-muted">
          I design and ship the automation, data flows, and operating cadence
          behind revenue and delivery. I stay close to the build and the rollout
          so teams can actually use what we ship.
        </p>
        <ul class="mt-6 grid gap-3 text-sm text-muted">
          <li>
            Built revenue and operations systems used by GTM and field teams
          </li>
          <li>
            Shipped live systems with uptime, support, and operational accountability
          </li>
          <li>
            Delivered internal tools used in daily sales, ops, and leadership workflows
          </li>
          <li>
            Owns end-to-end delivery: design, build, rollout, and adoption
          </li>
        </ul>
        <div class="mt-8 flex flex-wrap gap-3">
          <a
            class="focus-ring inline-flex w-full items-center justify-center rounded-lg bg-accent px-5 py-2.5 text-sm font-semibold text-white transition hover:bg-accent-dark sm:w-auto"
            href="/work"
          >
            View Work
          </a>
          <a
            class="focus-ring inline-flex w-full items-center justify-center rounded-lg border border-border px-5 py-2.5 text-sm font-semibold text-ink transition hover:border-ink sm:w-auto"
            href="/resume"
          >
            Download Resume
          </a>
        </div>
      </div>
      <div class="surface-muted p-6">
        <h2 class="text-xl font-semibold text-ink">How I help</h2>
        <p class="mt-2 text-muted">
          I connect GTM, product, engineering, and finance to build operating
          systems that can be run and maintained. I focus on execution and
          follow-through.
        </p>
        <div class="mt-4 grid gap-3 text-sm text-muted">
          <div class="surface p-4">
            <p class="font-semibold text-ink">Systems + automation</p>
            <p class="mt-1">
              Lead-to-cash, lifecycle ops, and cross-functional workflows.
            </p>
          </div>
          <div class="surface p-4">
            <p class="font-semibold text-ink">Program delivery</p>
            <p class="mt-1">
              Execution models that keep multi-team initiatives moving.
            </p>
          </div>
          <div class="surface p-4">
            <p class="font-semibold text-ink">Technical ownership</p>
            <p class="mt-1">
              Hands-on build work across data, tooling, and integrations.
            </p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Featured work: lead with the strongest proof -->
  <section class="py-16">
    <div class="page-container">
      <div class="max-w-2xl">
        <h2 class="text-3xl font-semibold text-ink">Featured work</h2>
        <p class="mt-3 text-muted">
          A few examples of how I work and what I deliver.
        </p>
      </div>
      <div class="mt-8 grid gap-5">
        <article class="surface p-6">
          <div class="flex flex-wrap items-center justify-between gap-2">
            <h3 class="text-xl font-semibold">Kansas Electric</h3>
            <span class="text-sm text-muted">Business + Apps</span>
          </div>
          <p class="mt-3 text-muted">
            Website rebuild, internal tooling, and automation to tighten intake
            and execution.
          </p>
          <a
            class="focus-ring mt-4 inline-flex text-sm font-semibold text-accent hover:text-accent-dark"
            href="/work/kansas-electric"
          >
            View case study
          </a>
        </article>
        <article class="surface p-6">
          <div class="flex flex-wrap items-center justify-between gap-2">
            <h3 class="text-xl font-semibold">Eden (MMO engineering)</h3>
            <span class="text-sm text-muted">Live systems</span>
          </div>
          <p class="mt-3 text-muted">
            Long-lived, production-grade software with reliability and
            performance constraints.
          </p>
          <a
            class="focus-ring mt-4 inline-flex text-sm font-semibold text-accent hover:text-accent-dark"
            href="/eden"
          >
            View engineering scope
          </a>
        </article>
        {caseStudies.map((study) => (
          <article class="surface p-6">
            <div class="flex flex-wrap items-center justify-between gap-2">
              <h3 class="text-xl font-semibold">{study.data.title}</h3>
              <span class="text-sm text-muted">{study.data.category}</span>
            </div>
            <p class="mt-3 text-muted">{study.data.summary}</p>
            <p class="mt-4 font-semibold">{study.data.impact}</p>
            <a
              class="focus-ring mt-4 inline-flex text-sm font-semibold text-accent hover:text-accent-dark"
              href={`/work/${study.slug}`}
            >
              Read the full case study
            </a>
          </article>
        ))}
      </div>
    </div>
  </section>

  <!-- Technical projects: architecture and tradeoffs -->
  <section class="bg-panel py-16">
    <div class="page-container">
      <div class="max-w-2xl">
        <h2 class="text-3xl font-semibold text-ink">Technical projects</h2>
        <p class="mt-3 text-muted">
          Systems work focused on architecture decisions, constraints, and
          outcomes.
        </p>
      </div>
      <div class="mt-8 grid gap-5 md:grid-cols-2">
        {technicalProjects.map((project) => (
          <article class="surface p-6">
            <h3 class="text-lg font-semibold">{project.data.title}</h3>
            <p class="mt-2 text-muted">{project.data.summary}</p>
            <p class="mt-4 text-sm text-muted">
              Key decision: {project.data.decisions[0]}
            </p>
            <a
              class="focus-ring mt-4 inline-flex text-sm font-semibold text-accent hover:text-accent-dark"
              href="/technical-projects"
            >
              See technical breakdown
            </a>
          </article>
        ))}
      </div>
    </div>
  </section>

  <!-- Skills + operating style -->
  <section class="py-16">
    <div class="page-container grid gap-8 lg:grid-cols-[1fr_0.9fr]">
      <div>
        <h2 class="text-3xl font-semibold text-ink">Skills & tooling</h2>
        <p class="mt-3 text-muted">
          Hands-on capability across engineering, data, operations, and GTM
          execution.
        </p>
        <a
          class="mt-4 inline-flex text-sm font-semibold text-accent hover:text-accent-dark"
          href="/skills"
        >
          View skills in practice
        </a>
      </div>
      <div class="surface p-6">
        <h3 class="text-lg font-semibold">How I work</h3>
        <ul class="mt-3 list-disc space-y-2 pl-5 text-muted">
          <li>Start with business signal flow and decision points</li>
          <li>Design the operating system before tooling</li>
          <li>Ship in small increments, not big-bang launches</li>
          <li>Own the results through rollout and adoption</li>
        </ul>
      </div>
    </div>
  </section>

  <!-- About preview -->
  <section class="bg-panel py-16">
    <div class="page-container">
      <div class="max-w-2xl">
        <h2 class="text-3xl font-semibold text-ink">About</h2>
        <p class="mt-3 text-muted">
          I bridge business and engineering to build systems that make execution
          reliable. I prefer clear ownership and accountable delivery.
        </p>
      </div>
      <a
        class="mt-4 inline-flex text-sm font-semibold text-accent hover:text-accent-dark"
        href="/about"
      >
        Read the full story
      </a>
    </div>
  </section>

  <!-- Contact CTA -->
  <section class="py-16">
    <div
      class="page-container flex flex-wrap items-center justify-between gap-6 rounded-2xl bg-ink px-6 py-10 text-white shadow-[0_18px_50px_rgba(15,23,42,0.25)] sm:px-8"
    >
      <div>
        <h2 class="text-3xl font-semibold">Let's build what scales</h2>
        <p class="mt-2 max-w-xl text-white/75">
          If you need a leader who can align teams, build systems, and ship
          outcomes, let's talk.
        </p>
      </div>
      <div class="flex flex-wrap gap-3">
        <a
          class="focus-ring inline-flex w-full items-center justify-center rounded-lg bg-white px-5 py-2.5 text-sm font-semibold text-ink sm:w-auto"
          href="mailto:eric@email.com"
        >
          eric@email.com
        </a>
        <a
          class="focus-ring inline-flex w-full items-center justify-center rounded-lg border border-white/30 px-5 py-2.5 text-sm font-semibold text-white sm:w-auto"
          href="/resume"
        >
          Resume
        </a>
        <a
          class="focus-ring inline-flex w-full items-center justify-center rounded-lg border border-white/30 px-5 py-2.5 text-sm font-semibold text-white sm:w-auto"
          href="https://www.linkedin.com/"
        >
          LinkedIn
        </a>
      </div>
    </div>
  </section>
</Base>
